{
  "name": "bex-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "bex.co MVP — the webhook → build → deploy → serve cycle (deploy-from-git half) on Docker/OrbStack",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "demo": "bash scripts/demo.sh"
  }
}
